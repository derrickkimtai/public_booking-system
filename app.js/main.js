// JavaScript code for handling user registration
document.getElementById('register-btn').addEventListener('click', function() {
    // handle user registration logic
  });
  
  // JavaScript code for handling user login
  document.getElementById('login-btn').addEventListener('click', function() {
    // handle user login logic
  });
  
  // JavaScript code for selecting a route
  var selectRouteButtons = document.getElementsByClassName('select-route-btn');
  for (var i = 0; i < selectRouteButtons.length; i++) {
    selectRouteButtons[i].addEventListener('click', function() {
      // handle route selection logic
    });
  }
  
  // JavaScript code for seat selection
  // Dynamically update seat layout and handle seat selection logic
  
  // JavaScript code for booking confirmation
  document.getElementById('confirm-booking-btn').addEventListener('click', function() {
    // handle booking confirmation logic
  });
  
  // JavaScript code for user dashboard
  // Dynamically update booking list and handle cancel booking logic